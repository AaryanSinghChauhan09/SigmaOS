const path = require('path');

module.exports = {
    ROOT_DIR: path.resolve(__dirname, '../../'),
    BUILD_DIR: path.resolve(__dirname, '../../build'),
    RELEASE_DIR: path.resolve(__dirname, '../../release')
};
