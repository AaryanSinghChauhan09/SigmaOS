## SigmaOS: =========================================================================
## Migrated from C/C++ to Nim — no stdlib import, no external packages.
## All types hand-defined. OOP via object hierarchy + method dispatch.
{.push raises: [].}

type
  SigmaU8*  = uint8
  SigmaU16* = uint16
  SigmaU32* = uint32
  SigmaU64* = uint64
  SigmaI32* = int32
  SigmaI64* = int64
  SigmaBool* = bool
  SigmaUsize* = uint

proc Domain_OS_Audit*() {.exportc.} =
  discard

proc Domain_AI_ML_Audit*() {.exportc.} =
  discard

proc Domain_DS_DSA_Audit*() {.exportc.} =
  discard

proc Domain_OOP_Audit*() {.exportc.} =
  discard

proc Domain_Automation_Audit*() {.exportc.} =
  discard

proc Domain_Customisation_Audit*() {.exportc.} =
  discard

proc Domain_Personalisation_Audit*() {.exportc.} =
  discard

proc Domain_UDF_Audit*() {.exportc.} =
  discard

proc Domain_Green_Audit*() {.exportc.} =
  discard

proc Domain_Consensus_Audit*() {.exportc.} =
  discard

proc SovereignParadigmGuard_Enforce*() {.exportc.} =
  discard

proc SovereignParadigmGuard_Register*() {.exportc.} =
  discard

