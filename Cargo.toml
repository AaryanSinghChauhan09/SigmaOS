[workspace]
resolver = "2"
members = [
    "kabi",
    "kernel",
    "sdk/driver",
    "sigma-sh",
    "suites/S03_Orchestrator",
    "tools/sigma-cli-bin",
    "tools/sigma-format",
    "tools/sigma-lint",
    "userland/agent",
    "userland/coreutils",
    "userland/sigpkg",
    "sigma_pkg_registry",
    "userland/system_api/control_center",
    "userland/system_api/ai_integration",
    "userland/system_api/dev_studio",
    "userland/ui/design_system",
    "userland/system_api/common",
]
default-members = ["kernel"]

[workspace.package]
version     = "15.0.0"
authors     = ["SigmaOS Project"]
edition     = "2021"
license     = "MIT"
repository  = "https://github.com/AaryanSinghChauhan09/SigmaOS"

[workspace.dependencies]
# No third-party crates in kernel — all dependencies are sigma-* internal

[profile.dev]
opt-level    = 1
debug        = true
overflow-checks = true
panic        = "abort"

[profile.release]
opt-level  = "z"     # size-optimised
lto        = true
codegen-units = 1
strip      = "symbols"
panic      = "abort"

[profile.pgo]
inherits   = "release"
opt-level  = 3       # Maximum optimization for PGO
lto        = "fat"   # Full LTO for PGO
codegen-units = 1
strip      = false  # Keep symbols for profiling
panic      = "abort"

[profile.bench]
inherits   = "release"
opt-level  = 3
lto        = true
codegen-units = 1
strip      = false
debug      = true

[profile.kernel]
inherits   = "release"
opt-level  = "s"
debug      = false
