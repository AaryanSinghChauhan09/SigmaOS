# Build artifacts
*.o
*.bin
*.iso
*.elf
build/
dist/
target/
.cache/
.pytest_cache/

# Generated per-developer — never commit (run cmake -B build -DCMAKE_EXPORT_COMPILE_COMMANDS=ON)
compile_commands.json

# Node / npm — always regenerated by npm ci
node_modules/
node_modules/.vite

# ISO staging and sigma-build output dirs
iso_root/
sigma-build/

# Editor/IDE
.vscode/
*.swp
*.swo

# OS junk
Thumbs.db
.DS_Store

# Temp scripts
patch.py
*.tmp

# Local junctions for compatibility
networking

# Wiki temp / local scratch only (wiki_repo/ is tracked for GitHub Wiki CI sync)
wiki_temp/
