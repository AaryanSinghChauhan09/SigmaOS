# SigmaOS Sovereign v2.0 Dependency Manifest
# Fulfills 'Professional Application' status across Windows/Linux/macOS.
# ----------------------------------------------------------------------
# 100% Zero-Dependency Core. All logic is shimmed by sigma_std.py

# Core Networking & Agentic Protocols
# requests >= 2.25.1    --> REPLACED BY SigmaNetwork (sigma_std.py)
# aiohttp >= 3.8.0      --> REPLACED BY SigmaNetwork (sigma_std.py)
# urllib3 >= 1.26.0     --> UNNECESSARY (using urllib.request natively)

# IO & File-System Mesh
# psutil >= 5.8.0       --> REPLACED BY SigmaSys (sigma_std.py)
# pycryptodome >= 3.10.1--> REPLACED BY SigmaCrypto (sigma_std.py)
# watchdog >= 2.1.0     --> PLANNED NATIVE REPLACEMENT

# AI/ML Tools (Sovereign Lab)
# numpy >= 1.20.0       --> REPLACED BY SigmaMath (sigma_std.py)
# scikit-learn >= 0.24.0 # Optional: High-Performance ML
# pandas >= 1.2.0        # Optional: Data Studio Logic

# Multimedia & Gaming Parity (UAL)
# pygame >= 2.0.0       # Optional: Logic Game Core
# Pillow >= 8.2.0       # Optional: Image Forge

# Performance & Analytics
# setuptools-scm
# typing-extensions >= 3.10.0
# importlib-metadata >= 4.0.0

# Git Mesh Sync
# GitPython >= 3.1.18 # Optional: if sync.ps1 is converted to python
