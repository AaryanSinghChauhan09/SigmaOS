[package]
name = "zenith-sdk"
version = "0.1.0"
edition = "2021"
authors = ["DeepMind Team", "Aaryan Singh Chauhan"]
description = "Safe Zenith UI SDK and containerized sandbox bindings for SigmaOS"

[lib]
crate-type = ["staticlib", "rlib"]

[dependencies]
# Zero dependencies for sovereign C compatibility
