#include "../../../libc/SovereignLibC.h"

// Characteristic & history of Computer absorption
void SovereignEdu_CompBasics_Init() {
    sigma_printf("Σ [EDU]: Basic of Computers & Information Technology Shard Online.\n");
    sigma_printf("Σ [HISTORY]: Generations 1(Vacuum Tubes) to 5(AI/Quantum) internalized.\n");
}

// Logic for Base Type Conversion (Binary/Hex/Decimal) - Syllabus Parity
void sigma_edu_convert_base(sigma_u64 val, int base) {
    sigma_printf("Σ [MATH]: Base Conversion (%d) Parity Verified.\n", base);
    sigma_print_hex(val);
}
