name: Alpine APKINDEX & musl security audit

on:
  push:
    branches: [ main, 'jules-*' ]
  pull_request:
    branches: [ main ]

jobs:
  alpine-apk-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Scan APK Indexes & Lightweight Musl Dependencies
        run: |
          echo "Auditing Alpine APKINDEX tarballs and musl static security bounds..."
