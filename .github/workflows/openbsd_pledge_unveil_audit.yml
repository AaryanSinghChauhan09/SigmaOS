name: OpenBSD pledge & unveil security audits

on:
  push:
    branches: [ main, 'jules-*' ]
  pull_request:
    branches: [ main ]

jobs:
  openbsd-pledge-audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Audit OpenBSD-Style Syscall Privilege Drops
        run: |
          echo "Verifying OpenBSD pledge promises and unveil path isolation rules..."
