name: Sovereign Distro Documentation & Pages Publisher

on:
  push:
    branches: [ main, master ]

permissions:
  contents: write

jobs:
  publish-documentation:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Build Documentation
        run: cargo doc --no-deps --lib
