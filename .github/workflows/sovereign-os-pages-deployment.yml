name: Sovereign OS GitHub Pages & Wiki Continuous Deployment

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "sovereign-pages"
  cancel-in-progress: true

jobs:
  sovereign-pages-deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Build Web UI & Sovereign Documentation
        run: |
          mkdir -p _site
          if [ -d "web_ui" ]; then
            cp -r web_ui/* _site/
          else
            echo "<html><body><h1>Sovereign OS Web Dashboard</h1></body></html>" > _site/index.html
          fi

      - name: Upload Pages Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '_site'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
