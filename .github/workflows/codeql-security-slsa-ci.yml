name: CodeQL Security Scanning & SLSA Provenance Assurance

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master ]
  schedule:
    - cron: '30 2 * * 0'

permissions:
  contents: read
  security-events: write

jobs:
  codeql-analysis:
    name: CodeQL Static Security Analysis
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Initialize CodeQL
        uses: github/codeql-action/init@6bb037a2cb56e23b0b14300929148f55c2f0175d # v3.28.10
        with:
          languages: 'actions, python, rust'

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@6bb037a2cb56e23b0b14300929148f55c2f0175d # v3.28.10
