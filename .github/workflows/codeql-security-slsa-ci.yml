name: CodeQL Security Scanning & SLSA Provenance Assurance

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master ]
  schedule:
    - cron: '30 2 * * 0'

permissions:
  contents: read
  security-events: write

jobs:
  codeql-analysis:
    name: CodeQL Static Security Analysis
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.2.2

      - name: Initialize CodeQL
        uses: github/codeql-action/init@df409f7d9260372ab5e19272a11f774b4e6d2961 # v3.28.11
        with:
          languages: 'actions, python, rust'

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@df409f7d9260372ab5e19272a11f774b4e6d2961 # v3.28.11
