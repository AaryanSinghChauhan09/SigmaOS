name: "Haiku & BeOS API Parity CI"

on:
  push:
    branches: [ "main", "dev", "jules-*" ]
  pull_request:
    branches: [ "main", "dev" ]

permissions:
  contents: read

jobs:
  haiku-parity-test:
    name: "Verify Haiku OS & BeOS API Parity"
    runs-on: ubuntu-latest
    steps:
      - name: "Checkout Code"
        uses: actions/checkout@v4

      - name: "Setup Rust Toolchain"
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable

      - name: "Run Haiku & BeOS API Subsystem Tests"
        run: |
          ./run_sigma_tests.sh
