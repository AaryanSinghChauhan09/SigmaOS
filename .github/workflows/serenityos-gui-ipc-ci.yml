name: "SerenityOS GUI & LibIPC Parity CI"

on:
  push:
    branches: [ "main", "dev", "jules-*" ]
  pull_request:
    branches: [ "main", "dev" ]

permissions:
  contents: read

jobs:
  serenity-parity-test:
    name: "Verify SerenityOS GUI & LibIPC Parity"
    runs-on: ubuntu-latest
    steps:
      - name: "Checkout Code"
        uses: actions/checkout@v4

      - name: "Setup Rust Toolchain"
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable

      - name: "Run SerenityOS GUI & IPC Tests"
        run: |
          ./run_sigma_tests.sh
