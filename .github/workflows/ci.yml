name: SigmaOS CI

on:
  push:
    branches: [ main, 'feat/**', 'fix/**', 'feature/**', 'perf/**' ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read
  checks: write
  pull-requests: read

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: "-D warnings"

jobs:
  # ── Compilation Check ──────────────────────────────────────────────────────
  check:
    name: Cargo Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
        with:
          fetch-depth: 0

      - name: Install Rust nightly
        uses: dtolnay/rust-toolchain@v1  # stable tag
        with:
          toolchain: nightly
          targets: x86_64-unknown-none
          components: rust-src, llvm-tools-preview

      - name: Cache Cargo registry
        uses: actions/cache@v4  # v4.2.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-

      - name: cargo check
        run: cargo +nightly check --all 2>&1

  # ── Build ──────────────────────────────────────────────────────────────────
  build:
    name: Build
    runs-on: ubuntu-latest
    needs: check
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
      - uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: nightly
          targets: x86_64-unknown-none
          components: rust-src
      - uses: actions/cache@v4  # v4.2.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-build-${{ hashFiles('**/Cargo.lock') }}
      - name: Build (debug)
        run: cargo +nightly build --all
      - name: Build (release)
        run: cargo +nightly build --all --release

  # ── Tests ──────────────────────────────────────────────────────────────────
  test:
    name: Tests
    runs-on: ubuntu-latest
    needs: check
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
      - uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: nightly
          components: rust-src
      - uses: actions/cache@v4  # v4.2.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-test-${{ hashFiles('**/Cargo.lock') }}
      - name: Run Rust tests
        run: cargo +nightly test --all
      - name: Install Python test dependencies
        run: pip3 install pytest
      - name: Run Python tests
        run: python3 -m pytest tests/ -v --timeout=60

  # ── Linting ────────────────────────────────────────────────────────────────
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
      - uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: nightly
          components: clippy, rustfmt
      - name: Clippy
        run: cargo +nightly clippy --all -- -D warnings 2>&1 || true
      - name: Format check
        run: cargo +nightly fmt --all -- --check 2>&1 || true

  # ── Linux & BSD Distro Innovations Verification ───────────────────────────
  distro-innovations:
    name: Distro Innovations Test Suite
    runs-on: ubuntu-latest
    needs: check
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
      - uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: stable
      - name: Test Sovereign NextGen Distro Leap Subsystem
        run: |
          mkdir -p build
          rustc --edition=2021 --test src/distro/sovereign_nextgen_distro_leap.rs -o build/test_nextgen_leap
          ./build/test_nextgen_leap
      - name: Test Open-Source Distro Innovations Subsystem
        run: |
          rustc --test --edition 2021 src/distro/open_source_distro_innovations.rs -o build/test_open_source_distro_innovations
          ./build/test_open_source_distro_innovations

  # ── Security Audit ─────────────────────────────────────────────────────────
  audit:
    name: Security Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
      - uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: stable
      - name: Install cargo-audit
        run: cargo install cargo-audit --locked
      - name: Audit dependencies
        run: cargo audit 2>&1 || true

  # ── Documentation ──────────────────────────────────────────────────────────
  docs:
    name: Documentation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
      - uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: nightly
          components: rust-src
      - name: Build documentation
        run: cargo +nightly doc --all --no-deps 2>&1 || true
