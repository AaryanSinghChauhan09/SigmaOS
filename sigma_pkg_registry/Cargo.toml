[package]
name = "sigma_pkg_registry"
version = "0.1.0"
edition = "2021"
description = "SigmaOS Package Registry - recipe loader and parser"

[dependencies]
# Zero external dependencies - all parsing implemented manually
