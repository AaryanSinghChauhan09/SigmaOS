#ifndef SIGMA_HOST_TYPES_H
#define SIGMA_HOST_TYPES_H

#include "sigma_libc.h"

// Types are already defined in sigma_libc.h (Defensive Edition)
// No need to include the kernel-side sigma_types.h to avoid recursion.

#endif
