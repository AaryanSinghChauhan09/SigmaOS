[package]
name = "sigma_media"
version.workspace = true
authors.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Native GPU terminal emulator rendering backend and sound/video media engine for SigmaOS, displacing Alacritty, Kitty, WezTerm, Fish, Zsh, FFmpeg, and PipeWire"

[dependencies]
# Standard library is available in userland
