# Generated method: SegmentDescriptor.__init__
from enum import Enum, auto
import secrets

class SegmentDescriptor:
    def __init__(self, base, limit, access):
        self.base = base
        self.limit = limit
        self.access = access