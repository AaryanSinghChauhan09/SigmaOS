[package]
name        = "sigma-virtualization"
description = "SigmaOS S11 Virtualization Shard — WASM JIT + VirtIO"
version.workspace = true
edition.workspace = true

[lib]
name = "sigma_virtualization"
crate-type = ["staticlib", "rlib"]

[features]
default = ["hosted"]
hosted  = []
