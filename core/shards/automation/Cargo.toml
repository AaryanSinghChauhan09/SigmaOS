[package]
name = "sigma-automation"
description = "SigmaOS Automation Shard — CI triggers, self-healing, daemon"
version.workspace = true
edition.workspace = true

[[bin]]
name = "sigma-daemon"
path = "src/main.rs"

[lib]
name = "sigma_automation"
crate-type = ["rlib"]
