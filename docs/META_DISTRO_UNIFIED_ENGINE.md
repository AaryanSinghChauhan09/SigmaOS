# Meta-Distro Unified Engine Specification

The Meta-Distro Unified Engine enables SigmaOS to ingest, translate, sandbox, and execute packages from foreign distributions (`.deb`, `.rpm`, `.pkg.tar.zst`, `.apk`, `.xbps`, `.pkg`) seamlessly within capability-gated containers.
