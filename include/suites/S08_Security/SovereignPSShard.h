/* S SIGMAOS: SOVEREIGN PS SHARD HEADER */
#ifndef SOVEREIGN_PS_SHARD_H
#define SOVEREIGN_PS_SHARD_H
#include "sigma_types.h"

void sigma_ps_gpu_push (const void* buffer, sigma_size_t size);
void SovereignPSShard_Init (void);

#endif
