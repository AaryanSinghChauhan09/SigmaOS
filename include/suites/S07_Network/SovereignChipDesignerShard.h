/* S SIGMAOS: SOVEREIGN CHIPDESIGNER SHARD HEADER */
#ifndef SOVEREIGN_CHIPDESIGNER_SHARD_H
#define SOVEREIGN_CHIPDESIGNER_SHARD_H
#include "sigma_types.h"

void sigma_chip_synthesize (const char* hdl_path);
void SovereignChipDesignerShard_Init (void);

#endif
